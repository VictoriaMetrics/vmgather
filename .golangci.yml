version: "2"

run:
  tests: false

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - ineffassign
  settings:
    errcheck:
      check-blank: false
  exclusions:
    paths:
      - build
      - tests
